<?php

    var blog_pg_dir = "../blog_pages";
    var page_count = 0;
    // This stuff might need to communicate with Javascript
    function retrieve_blog_pages()
    {
        var directory_contents = scandir("blog_pages/");
        //This should retrieve all the page files first then sort them and return an array with references to each file.
    }

    function sort_blog_pages()
    {

    }
